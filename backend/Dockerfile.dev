# Dev: install deps and run tsx watch (source mounted via compose)
FROM node:22-bookworm-slim
RUN apt-get update -y && apt-get install -y python3 make g++ && rm -rf /var/lib/apt/lists/*
WORKDIR /app
COPY package*.json ./
RUN npm install
CMD ["sh", "-c", "npm install && npm run dev"]
